import 'package:flutter/material.dart';

class ItemManagementScreen extends StatelessWidget {
  const ItemManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('물품 관리', style: TextStyle(fontSize: 20)),
    );
  }
}